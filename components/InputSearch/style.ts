import { StyleSheet } from "react-native"

export const Style = StyleSheet.create({
    container:{
        backgroundColor:'red',
        height: 40,
        width: 320,
        marginLeft:15
    }
})